{
  "name": "musegrid",
  "private": true,
  "scripts": {
    "dev": "corepack pnpm --filter @musegrid/web dev",
    "build": "corepack pnpm --filter @musegrid/web build",
    "lint": "corepack pnpm --filter @musegrid/web lint",
    "typecheck": "corepack pnpm -r typecheck",
    "test": "corepack pnpm -r test",
    "e2e": "corepack pnpm --filter @musegrid/web e2e"
  }
}
